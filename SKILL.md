---
name: ai-shelter-mini-open
description: "批量制作末日逃生避难所短视频工作流：首次调用先确认批次数量、主题方式、输出规格、生成范围、历史去重和交付方式；随后生成灾难与物品主题、双段15秒提示词、4张关键帧，并可在明确授权后调用可用视频工具。适用于AI庇护所、末日逃生、第一人称避难所和灾难短视频。"
metadata:
  short-description: "批量生成末日逃生避难所视频内容包"
---

# AI Shelter Mini Open

## 目标

把“灾难逃生 → 投出主题信标 → 庇护所显现 → 进入两个治愈空间”整理为可重复执行的短视频工作流。每期默认由两条 15 秒竖屏视频组成，总时长约 30 秒，并配套 4 张关键场景图、双语视频提示词、衔接说明和可选发布文案。

本 Skill 不绑定电脑、盘符、保存目录或特定视频平台。没有生图或视频工具时，仍可完整交付提示词和参考帧计划。

需要时读取：

- [references/workflow-spec.md](references/workflow-spec.md)：两段视频、4 张关键帧和交付清单。
- [references/prompt-continuity.md](references/prompt-continuity.md)：第一人称、因果链与空间连续性。
- [references/theme-pools.md](references/theme-pools.md)：灾难、物品、逃生方式、建筑和房间组合。

## 首次调用确认门槛

“首次调用”指当前对话中第一次启用本 Skill。首次调用时不要生成主题、图片、视频或文件。先展示并让用户明确确认下列配置：

1. **制作批次**：默认 1 期；支持一次规划或制作多期。
2. **主题方式**：用户指定“灾难 + 物品”，或默认随机给 6 组候选。
3. **视频结构**：默认每期 2 × 15 秒、竖版 9:16；可以只做内容包。
4. **生成范围**：默认生成策划、双语提示词和 4 张关键帧；实际视频默认不生成。
5. **视频工具与费用**：默认不调用付费或外部视频工具。若用户希望实际生成，必须确认具体工具、模型、时长、任务数量和可能费用。
6. **封面与发布文案**：默认生成标题与 Tags，不生成额外封面；可修改。
7. **历史去重**：默认关闭；只有用户提供历史目录、主题清单或已有作品时才开启。
8. **结果交付**：默认直接在当前对话返回。若宿主可写文件，可使用当前工作区相对目录；只有用户主动指定时才使用其目录。

示例：

```text
开始前请确认本次配置：
- 1 期
- 随机提供 6 组“灾难 + 主题物”候选
- 每期 2 × 15 秒，9:16
- 生成策划、双语提示词和 4 张关键帧
- 暂不提交付费视频任务
- 生成标题与 Tags，不额外生成封面
- 不读取历史目录去重
- 结果直接返回；如需保存，请告诉我目录

回复“确认”即可，也可以直接修改任意一项。
```

用户明确确认后才能开始。不要把沉默、模糊回答或以前某一期的选择视为当前授权。多期批量视频的付费授权必须写明总任务数；失败后的付费重试需要新的明确授权。

当前对话内再次调用时，可以沿用已确认配置，但执行前用一句话复述批次数、视频范围和付费状态。

## 阶段一 主题与场景确认

1. 读取 [references/theme-pools.md](references/theme-pools.md)。
2. 用户已经给出灾难和主题物时，以它们建立方案；只给出一项时，补充 6 个跨品类候选；选择随机时，提供 6 组“灾难 + 主题物”。
3. 同一批候选的物品类别、外形、功能和材质必须明显不同。主题物应能转译为手掌大小、单手快拆和一次投掷的应急信标。
4. 历史去重开启时，只读取用户明确提供的历史信息。历史信息不可读时，说明仅做本批次内部去重，不阻塞任务。
5. 用户选定主题后，随机或按要求确定：逃生方式、外部场景、灾难主体、擦肩险情、庇护所选址与体量、两个内部空间和两次互动。
6. 用一张确认卡汇总全部信息并等待一次确认。确认后连续完成静态内容包，不再逐项打断。

主题确认卡至少包含：

```text
批次编号：
灾难：
主题物与一级品类：
逃生方式与场景：
擦肩险情：
信标烟雾主色与辅色：
庇护所选址、体量和主题转译：
空间1与互动1：
空间2与互动2：
视频生成：不生成 / 工具与参数 / 已确认任务数
封面与发布文案：
交付方式：
```

## 阶段二 生成静态内容包

按 [references/workflow-spec.md](references/workflow-spec.md) 生成：

- 片段 A、片段 B 的中英文视频提示词。
- F1 至 F4 四张关键场景图的中英文提示词。
- 参考帧映射和两段衔接说明。
- 已确认需要时，调用当前宿主可用的图像生成工具制作 F1 至 F4。
- 标题、简介和 Tags；封面仅在首次配置已选择时生成。

所有视频提示词必须执行 [references/prompt-continuity.md](references/prompt-continuity.md)。F1 与 F2 是同一路线上的连续状态；F3 与 F4 是通过一扇真实门连接的两个空间，不是独立拼贴。

### 图片生成

1. 使用当前宿主的内置图片生成能力，分别生成 F1 至 F4，不用同一提示词的简单变体代替四个时间节点。
2. 默认 9:16、1K、RGB、不透明、无文字、无水印。
3. 相邻图片同时检查身份连续与时间推进。失败只重做不合格帧；但 F1/F2 任一空间关系失败时，应重新检查整对。
4. 若没有图片生成能力，交付完整提示词与构图计划，不伪造图片文件。

## 阶段三 可选视频生成

只有主题确认卡记录了本期明确授权，才能调用视频生成工具。

1. 使用用户确认的工具、模型、比例、分辨率和时长，不擅自切换平台或参数。
2. 片段 A 使用 F1、F2、F3；片段 B 只使用 F3、F4。严格保持顺序。
3. 先生成片段 A，技术成功后再生成片段 B。批量多期时按批次顺序执行，记录每个任务的提交状态。
4. 异步任务采用有上限的轮询，不无限等待、不无限重试。
5. 登录失败、余额不足、审核失败、容量限制或技术错误时立即停止新增任务，保留记录并报告。
6. 任何额外付费重试、改变模型或增加时长都需要用户再次明确确认。
7. “技术成功”只表示平台返回成功且文件可访问，不等同于内容质量验收。是否返工由用户观看后决定。

## 可选封面与发布文案

只有首次配置已选择封面，或用户在成片后明确追加，才生成封面。

可选方案：

- 无人物灾难海报：灾难现场、三个主题信标、短标题。
- 第一人称持物封面：一只紧张施力的裸手持主题信标，背景为灾难与对应庇护所，画面无文字。

发布文案包含：一个正式标题、5 个备选标题、简短简介和 8 至 12 个相关 Tags。不要把封面授权推定为视频重试授权，反之亦然。

## 结果交付与保存

默认直接在对话中交付。保存文件不是使用前提。

当用户要求保存时：

1. 优先使用用户指定目录。
2. 用户未指定但宿主有明确工作区时，可使用 `outputs/日期-灾难-主题/` 相对目录。
3. 没有明确工作区时只返回结果，不猜测电脑路径。
4. 可运行 [scripts/init_project.ps1](scripts/init_project.ps1)，并显式传入 `-Root`；不传时使用当前目录下的 `outputs`。
5. 不覆盖旧项目；同名项目使用 `-02`、`-03` 递增。
6. 完成后报告实际生成批次、图片数、视频数、失败状态和真实保存位置；没有保存时明确写“已在对话交付，未写入本地目录”。

## 完成标准

- 首次调用已获得完整配置确认。
- 每期只使用一个灾难与主题物组合。
- 两段提示词时间线、参考帧编号和动作状态一致。
- F1/F2 几何可达，F3/F4 通过真实门连续连接。
- 付费视频任务数量与用户授权一致，没有自动付费重试。
- 保存行为遵循用户选择，没有硬编码目录或工具路径。

